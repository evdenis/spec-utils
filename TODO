Опциии по умолчанию. Конфигурация.

исключение неиспользуемых полей структур
 Подструктуры в структурах
 Приоритет макросов для всего
 C::Field? класс
 C::Structure переписать внутренности
 Определять случаи, когда сама структура используется, но ни одно из её полей не задействована
 Определять случаи, когда enum используется, но ни одна из его констант нет

Разбор ACSL

Библиотеки для ACSL

PARSEC_PACKED workaround plugin?
раскрывать макросы, которые содержат attribute plugin?
удалять макросы, которые содержат attribute plugin?
